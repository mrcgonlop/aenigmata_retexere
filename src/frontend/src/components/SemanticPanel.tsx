// Milestone 3: shows the full lexical entry for a selected word —
// definitions grouped by source with provenance badges, usage attestations,
// morphological parse, and semantic drift warnings.
// See ARCHITECTURE.md — Layer 3: Exploration Interface.

export default function SemanticPanel() {
  return (
    <div className="p-4 text-gray-400 italic text-sm">
      SemanticPanel — not yet implemented
    </div>
  )
}
